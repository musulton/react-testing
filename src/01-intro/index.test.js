describe("dummy test", () => {
    test("should pass test", () => {
        // positif case

        // Arrange -> mock data
        // const data = [{name: "Enigma"}]

        // Action
        // <Component data={data} />

        // Assert
        // expect("text hello").toBeInTheDocument();
    })

    test("should fail test", () => {
        // negatif case

        // Arrange -> mock data

        // Action

        // Assert
    })
})
